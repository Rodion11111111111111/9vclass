const gate=document.querySelector('#site-gate');if(sessionStorage.getItem('siteAccess')==='yes')gate.hidden=true;document.querySelector('#site-gate-form').addEventListener('submit',async e=>{e.preventDefault();const s=document.querySelector('#site-gate-status');s.textContent='Проверяем…';const r=await fetch('/api/site-access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:document.querySelector('#site-password').value})});if(!r.ok)return s.textContent='Неверный пароль.';sessionStorage.setItem('siteAccess','yes');gate.hidden=true});
