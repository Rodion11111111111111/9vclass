const gate=document.querySelector('#site-gate');if(sessionStorage.getItem('siteAccess')==='yes')gate.hidden=true;document.querySelector('#site-gate-form').addEventListener('submit',async e=>{e.preventDefault();const s=document.querySelector('#site-gate-status'),b=e.currentTarget.querySelector('button'),c=new AbortController();s.textContent='Проверяем…';b.disabled=true;const t=setTimeout(()=>c.abort(),8000);try{const r=await fetch('/api/site-access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:document.querySelector('#site-password').value}),signal:c.signal});if(!r.ok){s.textContent='Неверный пароль.';return}sessionStorage.setItem('siteAccess','yes');gate.hidden=true}catch{s.textContent='Не удалось проверить пароль. Попробуй ещё раз.'}finally{clearTimeout(t);b.disabled=false}});
